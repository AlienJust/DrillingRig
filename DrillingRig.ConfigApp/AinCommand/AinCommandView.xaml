<UserControl x:Class="DrillingRig.ConfigApp.AinCommand.AinCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="3*"/>
				<ColumnDefinition Width="2*"/>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>
			<TextBlock Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" Text="Режим работы"/>
			<ComboBox Grid.Column="1" Grid.Row =" 0" ItemsSource="{Binding Path=AvailableModesetVariants, Mode=OneTime}" IsSynchronizedWithCurrentItem="True" SelectedItem="{Binding Path=SelectedModesetVariant, Mode=TwoWay}">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock VerticalAlignment="Center" Text="{Binding Path=Name, Mode=OneTime}"/>
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
			<TextBlock Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" Text="Уставка скорости"/>
			<xctk:IntegerUpDown Grid.Column="1" Grid.Row="1" Value="{Binding Path=Fset, Mode=TwoWay}"/>

			<TextBlock Grid.Column="0" Grid.Row="2" VerticalAlignment="Center" Text="Уставка момента"/>
			<xctk:IntegerUpDown Grid.Column="1" Grid.Row="2" Value="{Binding Path=Mset, Mode=TwoWay}"/>

			<TextBlock Grid.Column="0" Grid.Row="3" VerticalAlignment="Center" Text="Резерв (уставка 3)"/>
			<xctk:IntegerUpDown Grid.Column="1" Grid.Row="3" Value="{Binding Path=Set3, Mode=TwoWay}"/>

			<TextBlock Grid.Column="0" Grid.Row="4" VerticalAlignment="Center" Text="Минимальный момент (ограничение)"/>
			<xctk:IntegerUpDown Grid.Column="1" Grid.Row="4" Value="{Binding Path=Mmin, Mode=TwoWay}"/>

			<TextBlock Grid.Column="0" Grid.Row="5" VerticalAlignment="Center" Text="Максимальный момент (ограничение)"/>
			<xctk:IntegerUpDown Grid.Column="1" Grid.Row="5" Value="{Binding Path=Mmax, Mode=TwoWay}"/>
			
			<Button Grid.Row="6" Grid.Column="1" Content="Записать" Command="{Binding Path=SendAinCommand, Mode=OneTime}"/>
		</Grid>
		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>
			
		</Grid>
	</Grid>
</UserControl>
