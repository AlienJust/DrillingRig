<UserControl x:Class="DrillingRig.ConfigApp.NewLook.Archive.ArchivesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrillingRig.ConfigApp.NewLook.Archive"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<local:ArchiveView Grid.Column="0" DataContext="{Binding Path=Archive1Vm, Mode=OneTime}"/>
		<local:ArchiveView Grid.Column="1" DataContext="{Binding Path=Archive2Vm, Mode=OneTime}"/>
	</Grid>
</UserControl>
