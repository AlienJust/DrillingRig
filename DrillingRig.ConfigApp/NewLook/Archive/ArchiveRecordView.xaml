<UserControl x:Class="DrillingRig.ConfigApp.NewLook.Archive.ArchiveRecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrillingRig.ConfigApp.NewLook.Archive"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2*"/>
			<ColumnDefinition/>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<TextBlock Grid.Column="0" Text="{Binding Time, Mode=OneTime}"/>
		<TextBlock Grid.Column="1" Text="{Binding FaultState, Mode=OneTime}"/>
		<TextBlock Grid.Column="2" Text="{Binding Mcw, Mode=OneTime}"/>
		<TextBlock Grid.Column="3" Text="{Binding Msw, Mode=OneTime}"/>
	</Grid>
</UserControl>
